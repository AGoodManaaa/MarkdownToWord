# Implementation Plan

- [x] 1. 修复重复初始化问题
  - [x] 1.1 移除 gui.py 中重复的 Feature 初始化语句
    - 删除 Phase 4 部分重复的 `self.tab_manager = TabManagerFeature(self)` 和 `self.pdf_export_feature = PDFExportFeature(self)`
    - 删除导入语句中重复的 `TabManagerFeature` 和 `PDFExportFeature`
    - _Requirements: 1.1, 1.2_

- [x] 2. 创建 FeatureRegistry 模块
  - [x] 2.1 创建 ui/feature_registry.py 文件
    - 实现 FeatureRegistry 类
    - 实现 register() 方法，检测重复注册并记录警告
    - 实现 get() 方法获取已注册 Feature
    - 实现 initialize_all() 方法批量初始化
    - _Requirements: 1.1, 1.3_
  - [x] 2.2 编写 FeatureRegistry 属性测试
    - **Property 1: Feature 单例性**
    - **Validates: Requirements 1.1**
  - [x] 2.3 编写 FeatureRegistry 单元测试
    - 测试重复注册警告
    - 测试 get() 返回正确实例
    - _Requirements: 1.1, 1.3_

- [x] 3. 创建 KeyboardShortcutsManager 模块
  - [x] 3.1 创建 ui/keyboard_shortcuts.py 文件
    - 实现 KeyboardShortcutsManager 类
    - 定义 DEFAULT_SHORTCUTS 字典
    - 实现 load_from_config() 方法
    - 实现 bind_all() 方法
    - 实现 _resolve_action() 方法解析 action 字符串
    - _Requirements: 3.1, 3.3_
  - [x] 3.2 编写 KeyboardShortcutsManager 属性测试
    - **Property 2: 快捷键配置一致性**
    - **Validates: Requirements 3.3**
  - [x] 3.3 编写 KeyboardShortcutsManager 单元测试
    - 测试默认快捷键绑定
    - 测试配置覆盖
    - _Requirements: 3.1, 3.3_

- [x] 4. 创建 UI Builder 模块
  - [x] 4.1 创建 ui/app_builder.py 文件
    - 实现 UIBuilder 类作为统一入口
    - 实现 HeaderBuilder 类
    - 实现 MainContentBuilder 类
    - _Requirements: 2.1, 2.2, 2.3_
  - [x] 4.2 将 gui.py 中的 _create_header() 代码迁移到 HeaderBuilder
    - 保持相同的视觉效果
    - _Requirements: 2.1, 2.4_
  - [x] 4.3 将 gui.py 中的 _create_main_content() 代码迁移到 MainContentBuilder
    - 包括 _create_sidebar_content(), _create_input_panel(), _create_preview_panel()
    - _Requirements: 2.2, 2.4_

- [x] 5. 重构 gui.py 使用新模块
  - [x] 5.1 修改 App.__init__() 使用 FeatureRegistry
    - 替换直接初始化为 registry.register() 调用
    - _Requirements: 1.1_
  - [x] 5.2 修改 App._init_ui() 使用 UIBuilder
    - 替换 _create_header() 等调用为 UIBuilder.build()
    - _Requirements: 2.1, 2.2, 2.3_
  - [x] 5.3 修改快捷键绑定使用 KeyboardShortcutsManager
    - 移除 _init_ui() 中的 self.bind() 调用
    - 使用 KeyboardShortcutsManager.bind_all()
    - _Requirements: 3.1_

- [x] 6. Checkpoint - 确保代码结构重构后应用正常运行
  - Ensure all tests pass, ask the user if questions arise.

- [x] 7. 创建 ImageCache 模块
  - [x] 7.1 创建 ui/image_cache.py 文件
    - 实现 ImageCache 类
    - 实现 LRU 缓存策略
    - 实现 get() 和 put() 方法
    - 实现 _load_index() 和 _save_index() 持久化
    - _Requirements: 6.1, 6.2, 6.3, 6.4_
  - [x] 7.2 编写 ImageCache 属性测试
    - **Property 8: 缓存命中**
    - **Property 9: LRU 缓存淘汰**
    - **Property 10: 缓存持久化**
    - **Validates: Requirements 6.2, 6.3, 6.4**
  - [x] 7.3 编写 ImageCache 单元测试
    - 测试缓存存取
    - 测试 LRU 淘汰
    - 测试持久化恢复
    - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [x] 8. 创建 ImageDownloader 模块
  - [x] 8.1 创建 ui/image_downloader.py 文件
    - 实现 ImageDownloader 类
    - 使用 ThreadPoolExecutor 实现异步下载
    - 实现 download_async() 方法
    - 集成 ImageCache
    - _Requirements: 5.1, 5.3, 5.4_
  - [x] 8.2 编写 ImageDownloader 属性测试
    - **Property 6: 异步下载非阻塞**
    - **Property 7: 并发下载限制**
    - **Validates: Requirements 5.1, 5.3, 5.4**
  - [x] 8.3 编写 ImageDownloader 单元测试
    - 测试异步下载回调
    - 测试并发限制
    - 测试错误处理
    - _Requirements: 5.1, 5.3, 5.4, 5.5_

- [x] 9. 集成异步图片下载到 utils.py
  - [x] 9.1 修改 utils.py 中的 resolve_image_path() 函数
    - 添加异步下载支持
    - 添加缓存集成
    - 保持向后兼容（同步模式仍可用）
    - _Requirements: 5.1, 6.1_
  - [x] 9.2 修改 handlers.py 中的 ImageHandler 使用异步下载
    - 在预览中显示占位符
    - 下载完成后更新预览
    - _Requirements: 5.2, 5.3_

- [x] 10. Checkpoint - 确保异步图片下载正常工作
  - Ensure all tests pass, ask the user if questions arise.

- [x] 11. 优化预览渲染性能
  - [x] 11.1 优化 PreviewSyncFeature 的防抖和节流参数
    - 调整 debounce_ms 和 throttle_ms 参数
    - 添加配置支持
    - _Requirements: 4.2_
  - [x] 11.2 编写预览性能属性测试
    - **Property 3: 预览渲染性能**
    - **Property 4: 防抖有效性**
    - **Validates: Requirements 4.1, 4.2**
  - [x] 11.3 编写预览性能单元测试
    - 测试大文档渲染时间
    - 测试防抖机制
    - _Requirements: 4.1, 4.2_

- [x] 12. 实现增量预览渲染（可选优化）
  - [x] 12.1 创建 IncrementalPreviewRenderer 类
    - 实现块级差异计算
    - 实现增量更新逻辑
    - _Requirements: 4.3_
  - [x] 12.2 编写增量渲染属性测试
    - **Property 5: 增量渲染正确性**
    - **Validates: Requirements 4.3**
  - [x] 12.3 集成到 MarkdownPreview 组件
    - 替换全量渲染为增量渲染
    - _Requirements: 4.3, 4.4_

- [x] 13. Final Checkpoint - 确保所有优化正常工作
  - Ensure all tests pass, ask the user if questions arise.
